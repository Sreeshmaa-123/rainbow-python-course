title: rainbow-python
description: using python modules and libraries we are gonna create a rainbow
template:
  repo: rainbow-python-template
  name: rainbow-python
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
